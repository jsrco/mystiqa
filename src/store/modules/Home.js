const state = {
  Home: null
};

const getters = {
  getHome: state => {
    return state.Home;
  }
};

const mutations = {

};

export default {
  state,
  getters,
  mutations
};
